<%- include('header',data.title) %>

<p class="my-2"><%= data.message %></p>
<table>
  <thead>
  </thead>
  <tr><th>Name</th><th>Mail</th></tr>
  <% for(var item of data.db){ %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.mail %></td>
    </tr>
  <% }%>
</table>

<%- include("footer") %>
